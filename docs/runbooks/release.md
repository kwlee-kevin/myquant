# Runbook: Release (Lightweight)

## Pre-merge
- PR linked to spec/ADR (or `N/A` with reason)
- Required checks green
- Coverage checks green

## Merge
- Merge to `main` through PR only.

## Post-merge
- Confirm CI success on `main`.
- Verify key endpoints/flows in dev environment.
